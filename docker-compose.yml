services:
  backend:
    build: ./backend
    restart: always
    ports:
      - "127.0.0.3:8000:8000"
    networks: 
      - dev


  frontend:
    restart: always
    build: ./frontend
    ports:
      - "127.0.0.3:8001:8001"
    networks:
      - dev

networks:
  dev:
    driver: bridge
